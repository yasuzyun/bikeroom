  <div class="mt-3">
     <span>現在ログイン中のユーザー：<%= current_customer.first_name %></span>
    <p><%= link_to "投稿一覧", articles_path(@articles), class:"btn btn-success ml-5"%></p>
        </div>
      <h2 class="mt-3">投稿</h2>
            <%= form_with model: Article.new, local: true do |f|  %>
              <div class="form-group">
                <label for="articles_title">Title</label>
                <%= f.text_field :title , class: "form-control book_title"%>
              </div>
              
              <div class="form-group">
                <label for ="articles_opinion">Opinion</label>
                  <%= f.text_area :body , class: "form-control book_body"%>
              </div>
              
   

  <script src="https://maps.googleapis.com/maps/api/js?key=<%= ENV['API_KEY'] %>&callback=initMap" async defer></script>
  <script>
    function makeMap(lat, lng) {    
    var canvas = document.getElementById('map-canvas');
      var latlng = new google.maps.LatLng(lat, lng);
　  var mapOptions = {  // マップのオプション
        zoom: 17,
        center: latlng,
    };
    var map = new google.maps.Map(canvas, mapOptions); //作成
    return map;
  }
   window.onload = function(){
      makeMap(35.446806, 139.636163); 
  };
  </script>
   <style>
   #map-canvas {
     width: 600px;
     height: 400px;
   }
  </style>
  <div id="map-canvas" width="600px" height="400px">ここにマップが表示されます</div>
  <div class="form-group">
   <%= f.label :address, '場所' %>
   <%= f.text_field :address, class: 'form-control' %>
 </div>
              
              <div class="form-group">
                <%= f.submit "投稿する", class:  "btn btn-success"%>
              </div>
            <% end %>
 
     </div>
    <div class="col-md-8 offset-md-1">

    </div>
  </div>
</div>